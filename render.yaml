services:
  hyper-video-service:
    type: web
    name: hyper-video-service
    runtime: docker
    repo: https://github.com/TWilson63/hyper-video-service
    branch: main
    plan: starter
    envVars:
      - key: NODE_ENV
        value: production
      - key: MCP_TRANSPORT
        value: streamable-http
      - key: HYPER_VIDEO_API_KEY
        sync: false  # Generate a secure random key and set in Render dashboard